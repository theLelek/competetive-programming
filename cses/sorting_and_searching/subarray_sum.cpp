#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n; int x; cin >> n; cin >> x;
    vector<int> numbers;
    while (n--) {
        int current;
        cin >> current;
        numbers.push_back(current);
    }
    for (int l )

    return 0;
}